MPLAB C18 Run-Time Library Source Code

Directory structure:

lib			Installed library binaries
h			Installed header files
src			Library source code
  traditional		Non-extended mode library source code
  extended		Extended mode library source code

Build scripts:

src/makeall.bat		Rebuild all extended and non-extended mode libraries
			invokes traditional/makeall.bat and 
			extended/makeall.bat
src/<mode>/makeall.bat	Rebuild all libraries for <mode> where mode is
			traditional or extended. The startup code, C support
			library, and part specific library are rebuilt from
			source and installed.
src/<mode>/makeclib.bat	Rebuild and install the C support library. This
			includes the compiler math library, the Standard C
			Library, and the delay library.
src/<mode>/makestartup.bat  Rebuild and install the startup code.
src/<mode>/makeplib.bat	Rebuild and install the processor specific library
			for each supported device by invoking the appropriate
			makeonepXXXX.bat build script for each device.
src/<mode>makeonepXXXX.bat  Rebuild and install the processor specific library
			for the device specified as the first (and only)
			command line argument to the script. makeplib.bat
			specifies which script is used for which processors.
